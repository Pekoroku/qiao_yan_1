# 已知限制与上线前事项

本包是结构／交互阶段版本，不是最终视觉或正式上线验收。没有连接 GitHub 账号，没有推送或修改域名。

1. 尚未收到真实作品、艺术家介绍、照片、经历及联系方式。当前 50 个不同编号色块仅用于布局和维护验证；不是 AI 代造的油画。正式模式不读取这些样本。
2. 首页已按主视觉 1 + 精选 8 实现。最终画作选择、顺序、配色、尺寸节奏和动效仍需收到真实作品后与你确认。
3. 本地 Chromium 检查已执行；Firefox、WebKit、iPhone/iPad Safari 真机、安卓真机和真实浏览器 200% 缩放尚未验收。模拟屏宽不能代替真机。
4. 没有正式域名、GitHub Actions 运行、HTTPS 分享卡片、中国大陆多网络访问、真实托管迁移或回滚证据。
5. 色块图片极小，网络记录只能验证请求行为，不能证明真实油画的画质、色彩、首屏速度或流量预算通过。替换作品后必须重新测量 LCP、CLS、INP、冷缓存传输和色彩对照。
6. 图片可被保存。noindex、防止母版进入 dist、草稿状态都不是公开仓库的隐私保护；批准公开之前请将素材放在仓库外。已公开历史不能靠撤下网页收回。
7. 当前维护方式是 Markdown + JSON + 构建，无在线管理后台。可用 VS Code 或其他文本编辑器，也可把资料连同最新源码交给 AI。没有 CMS、账号、数据库、付款或假联系表单。
8. 改内容/图片后需重启开发服务；上线更新需重新构建。改 slug 暂无自动跳转；上线后应保持地址稳定。正式模式不允许空站，0 件空状态只用于演示测试。
9. 生成目录会先移动至被 Git 忽略的 `.cache/retired/`，便于排查旧产物。该目录不发布，可能随构建次数增大；停止开发/测试后可在文件管理器删除整个 `.cache`，下次会重新生成。
10. 包内 `dist` 为根路径本地预览构建。GitHub 仓库子路径必须由附带的工作流重新构建，不能直接上传这份预览 dist。

上述未测项保留为正式发布前检查，不代表不存在问题。用户提供正式素材和部署目标后继续完成。
